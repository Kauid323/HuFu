<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HuFu.Pages.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HuFu.Pages"
    xmlns:converters="using:HuFu.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:RefererImageConverter x:Key="RefererImageConverter" />
    </Page.Resources>

    <NavigationView x:Name="NavView" IsBackButtonVisible="Collapsed" IsSettingsVisible="False" Background="Transparent">
        <!-- MenuItems 将在代码中动态加载 -->
        <NavigationView.MenuItems>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem Content="我的" Tag="profile">
                <NavigationViewItem.Icon>
                    <ImageIcon Source="{x:Bind ViewModel.AvatarUrl, Mode=OneWay, Converter={StaticResource RefererImageConverter}}" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="设置" Tag="settings">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Setting" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Frame x:Name="ContentFrame" />
    </NavigationView>
</Page>
