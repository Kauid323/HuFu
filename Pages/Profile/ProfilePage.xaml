<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HuFu.Pages.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HuFu.Pages"
    xmlns:converters="using:HuFu.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:RefererImageConverter x:Key="RefererImageConverter" />
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Padding="40" Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="个人资料" FontSize="28" FontWeight="Bold" />

            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <PersonPicture Width="100" Height="100" 
                               DisplayName="{x:Bind ViewModel.Nickname, Mode=OneWay}"
                               ProfilePicture="{x:Bind ViewModel.AvatarUrl, Mode=OneWay, Converter={StaticResource RefererImageConverter}}" />

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="{x:Bind ViewModel.Nickname, Mode=OneWay}" FontSize="20" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="UID:" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.Uid, Mode=OneWay}" IsTextSelectionEnabled="True" />
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="手机号" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Mobile, Mode=OneWay}" IsTextSelectionEnabled="True" />
                    </Grid>
                    
                    <MenuFlyoutSeparator />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="角色" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Grid.Column="1" Text="普通用户" />
                    </Grid>
                </StackPanel>
            </Border>

            <Button Content="退出登录" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Padding="0,10" />
        </StackPanel>
    </ScrollViewer>
</Page>
