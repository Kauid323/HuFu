<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HuFu.Pages.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HuFu.Pages"
    xmlns:converters="using:HuFu.Converters"
    xmlns:controls="using:HuFu.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:RefererImageConverter x:Key="RefererImageConverter" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ConversationColumn" Width="280" MinWidth="200" MaxWidth="500" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 会话列表区 -->
        <Grid Grid.Column="0">
            <ListView x:Name="ConversationListView" 
                      ItemsSource="{x:Bind ViewModel.Conversations, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedConversation, Mode=TwoWay}"
                      SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ConversationDisplayItem">
                        <Grid Margin="0,8" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- 头像 -->
                            <PersonPicture ProfilePicture="{x:Bind AvatarUrl, Mode=OneWay, Converter={StaticResource RefererImageConverter}}" 
                                           DisplayName="{x:Bind Name, Mode=OneWay}"
                                           Width="40" Height="40" 
                                           VerticalAlignment="Center"/>

                            <!-- 名称和消息内容 -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" 
                                               FontWeight="SemiBold" 
                                               FontSize="14"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="1" 
                                               Text="{x:Bind TimeString, Mode=OneWay}" 
                                               FontSize="11" 
                                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                               Margin="8,0,0,0"/>
                                </Grid>
                                <TextBlock Text="{x:Bind LastMessage, Mode=OneWay}" 
                                           FontSize="13" 
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           MaxLines="1"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- 原生拖动分隔条 -->
        <Rectangle Grid.Column="1" 
                   Width="1" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Stretch"
                   Fill="{ThemeResource SystemControlForegroundBaseLowBrush}"
                   PointerEntered="Splitter_PointerEntered"
                   PointerExited="Splitter_PointerExited"
                   PointerPressed="Splitter_PointerPressed"
                   PointerMoved="Splitter_PointerMoved"
                   PointerReleased="Splitter_PointerReleased" />

        <!-- 聊天界面 -->
        <Grid Grid.Column="2" x:Name="ChatDetailGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 顶栏 -->
            <Grid Grid.Row="0" Padding="16,10" ColumnSpacing="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="2">
                    <TextBlock Text="{x:Bind ViewModel.ActiveConversationTitle, Mode=OneWay}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{x:Bind ViewModel.ActiveConversationSubtitle, Mode=OneWay}"
                               FontSize="12"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                </StackPanel>

                <Button Grid.Column="1" MinWidth="36" MinHeight="36" Padding="0" Background="Transparent">
                    <Button.Content>
                        <FontIcon Glyph="&#xE712;" />
                    </Button.Content>
                </Button>
            </Grid>

            <!-- 消息区 -->
            <Grid Grid.Row="1">
                <ListView x:Name="MessageListView" 
                          ItemsSource="{x:Bind ViewModel.Messages, Mode=OneWay}"
                          SelectionMode="None">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel VerticalAlignment="Bottom" ItemsUpdatingScrollMode="KeepLastItemInView" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:MessageDisplayItem">
                            <Grid Padding="10,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <PersonPicture DisplayName="{x:Bind SenderName, Mode=OneWay}"
                                               ProfilePicture="{x:Bind SenderAvatarUrl, Mode=OneWay, Converter={StaticResource RefererImageConverter}}"
                                               Width="32" Height="32"
                                               VerticalAlignment="Top" />

                                <StackPanel Grid.Column="1" Margin="10,0,0,0" Spacing="2" HorizontalAlignment="Left">
                                    <Grid HorizontalAlignment="Left">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{x:Bind SenderName, Mode=OneWay}"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{x:Bind TimeString, Mode=OneWay}"
                                                   FontSize="11"
                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                   Margin="8,0,0,0" />
                                    </Grid>

                                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                                            CornerRadius="8" 
                                            Padding="10,8"
                                            HorizontalAlignment="Left"
                                            MaxWidth="400">
                                        <TextBlock Text="{x:Bind Text, Mode=OneWay}" 
                                                   TextWrapping="WrapWholeWords" 
                                                   IsTextSelectionEnabled="True" />
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12"
                            Visibility="{x:Bind ViewModel.EmptyHintVisibility, Mode=OneWay}">
                    <SymbolIcon Symbol="Message" RenderTransformOrigin="0.5,0.5">
                        <SymbolIcon.RenderTransform>
                            <ScaleTransform ScaleX="2" ScaleY="2" />
                        </SymbolIcon.RenderTransform>
                    </SymbolIcon>
                    <TextBlock Text="选择一个会话开始聊天"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                </StackPanel>
            </Grid>

            <controls:ChatInputControl Grid.Row="2" />
        </Grid>
    </Grid>
</Page>
